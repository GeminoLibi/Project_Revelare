Optional helpers. Run from the repo root.

- `utilities\build_exe.bat` - Windows standalone exe (`dist/ProjectRevelare.exe`, gitignored)
- `python utilities\clean_all_cases.py` - batch clean of case findings
- `python utilities\reprocess_all_cases.py` - batch reprocess of cases
